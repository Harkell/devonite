<%= form_tag charges_path do %>
  <article>
    <label class="amount">
      <p>Amount: £50.00 per month</p>
      <span>You will automatically be billed £50 on a monthly basis.</span>
    </label>
  </article>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-description="Subscribe to devonite"
          data-amount="5000"
          data-currency="gbp"></script>
<% end %>